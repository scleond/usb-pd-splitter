# Custom footprints

Store project-specific KiCad footprint libraries (`.pretty` directories) here.

Use `${KIPRJMOD}/hardware/footprints` when adding this directory to the project's
footprint library table. Prefer vendor or manufacturer names in footprint-library
names when a library is specific to a part family.
