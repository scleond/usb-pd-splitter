# 3D models

Store project-specific component models here. Keep model filenames tied to the
component part number and reference them from footprints with a project-relative
`${KIPRJMOD}` path.

Do not add models until the associated footprint and part selection are stable.
